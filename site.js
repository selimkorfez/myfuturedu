const menuButton=document.querySelector(".menu-button");const navLinks=document.querySelector(".nav-links");if(menuButton&&navLinks){menuButton.addEventListener("click",()=>{const open=navLinks.classList.toggle("open");document.body.classList.toggle("menu-open",open);menuButton.setAttribute("aria-expanded",String(open))});navLinks.querySelectorAll("a").forEach(a=>a.addEventListener("click",()=>{navLinks.classList.remove("open");document.body.classList.remove("menu-open");menuButton.setAttribute("aria-expanded","false")}))}document.querySelectorAll("[data-year]").forEach(el=>el.textContent=new Date().getFullYear());const cookieBanner=document.querySelector(".cookie-banner");const cookieChoice=localStorage.getItem("mfe-cookie-choice");if(cookieBanner&&!cookieChoice)cookieBanner.classList.add("show");document.querySelectorAll("[data-cookie-choice]").forEach(btn=>btn.addEventListener("click",()=>{localStorage.setItem("mfe-cookie-choice",btn.dataset.cookieChoice);if(cookieBanner)cookieBanner.classList.remove("show")}));const form=document.querySelector("#lesson-enquiry");if(form){form.addEventListener("submit",e=>{e.preventDefault();const data=new FormData(form);const subject=encodeURIComponent("My Future Education lesson enquiry");const body=encodeURIComponent(`Parent/carer: ${data.get("parent")}\nEmail: ${data.get("email")}\nChild's first name: ${data.get("child")}\nAge/Year group: ${data.get("age")}\nLevel: ${data.get("level")}\nLesson format: ${data.get("format")}\nPreferred timing: ${data.get("timing")}\n\nMessage:\n${data.get("message")||""}`);window.location.href=`mailto:info@myfuturedu.com?subject=${subject}&body=${body}`;const status=form.querySelector(".form-status");if(status){status.style.display="block";status.textContent="Your email app should open with the enquiry ready to send."}})}